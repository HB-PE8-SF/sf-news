{% extends 'base.html.twig' %}

{% block title %}
  {{ parent() }} - Derniers articles
{% endblock %}

{% block content %}
  <div class="example-wrapper">
    <h1>{{ welcome_text }} ✅</h1>

    <p>{{ students|length }} étudiants</p>

    <div>{{ students|join(', ') }}</div>

    <div>
      {% for student in students %}
        <p>{{ student }}</p>
      {% else %}
        <p>Aucun étudiant trouvé</p>
      {% endfor %}
    </div>

    <div>
      {% for studentObj in studentsObj %}
        <p>{{ studentObj.name }}</p>
      {% endfor %}
    </div>

    <div>
      This friendly message is coming from:<ul>
        <li>
          Your controller at <code>/home/lucas/symfony/hb_pe8_sf_news/src/Controller/IndexController.php</code>
        </li>
        <li>
          Your template at <code>/home/lucas/symfony/hb_pe8_sf_news/templates/index/index.html.twig</code>
        </li>
      </ul>
    </div>
  </div>
{% endblock %}
